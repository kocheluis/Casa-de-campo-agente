# Workflow: <name>

> Copy this file to `workflows/<verb_noun>.md` and fill it in. Keep it in plain
> language — brief it the way you'd brief a teammate.

## Objective
What this workflow accomplishes and when to run it.

## Inputs
What the agent needs before starting (parameters, files, credentials).
- `input_name` — description, where it comes from

## Tools
Which scripts in `tools/` to use, in order.
1. `tools/<script>.py` — what it does, how to call it

## Steps
1. Step-by-step sequence the agent follows.
2. ...

## Output
Where the deliverable goes (cloud service, file path) and what it should look like.

## Edge cases & lessons learned
- Known failure modes, rate limits, timing quirks. Update this as you learn.
